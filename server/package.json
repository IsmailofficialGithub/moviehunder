{
  "name": "flick-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Catalog API (Cloudflare Worker) + local play relay",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "wrangler dev --port 8787",
    "dev:relay": "node play-relay.mjs",
    "deploy": "wrangler deploy",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:reload": "pm2 reload ecosystem.config.cjs",
    "pm2:stop": "pm2 stop moviehunter-api moviehunter-relay"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
